import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  headerContainer: {
    height: 'auto',
    alignItems: 'center',
    padding: 15,
    flexDirection: 'row',
  },
  headerTitle: {
    flex: 1,
    textAlign: 'center',
    fontSize: 17,
    color: 'black',
    fontWeight: '600',
  },
  headerIcon: {
    height: 24,
    width: 24,
  },
});
export default styles;
